server.port: 8888

spring:
  application.name: utils-config-server
  cloud:
    service-registry:
      auto-registration:
        enabled: false
    config:
      server:
        native:
          search-locations: /Users/clavaud/Development/DevArea/wannaenjoy/config-files

javamelody:
  enabled: false

logging:
  file:
    name: ./logs/utils-config-server.log

info.tags.environment: development
